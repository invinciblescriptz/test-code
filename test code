local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/memejames/elerium-v2-ui-library/main/Library"))()
local window = library:AddWindow("INV Public || made by invincible", {
    main_color = Color3.fromRGB(0, 0, 0),
    min_size   = Vector2.new(660, 520),
    can_resize = false,
})

local infoTab = window:AddTab("Information")

infoTab:AddLabel("Invincible loves Ninja Turtles").TextSize = 24
infoTab:AddLabel("Made by Invincible").TextSize = 22
infoTab:AddLabel("More features coming soon 🔜").TextSize = 22

-- Services
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local Lighting = game:GetService("Lighting")

local muscleEvent = LocalPlayer:WaitForChild("muscleEvent", 8)
getgenv().NexusRunning = true

-- Globals & states
local states = {
    AutoPushups = false,
    AutoWeight = false,
    AutoHandstands = false,
    FastPunch = false,
    FastStrength = false,
    FastRebirth = false,
    InfiniteJump = false,
    AntiAFK = false,
    AutoRebirth = false,
    AutoRebirthTarget = false,
    KillAura = false,
    AutoKillSelected = false,
}

local targetRebirths = 1
local selectedTarget = nil
local jumpConn, antiAfkConn, lockConn, followTarget, following = nil, nil, nil, nil, false

-- Helper functions
local function tpTo(pos)
    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.CFrame = CFrame.new(pos)
    end
end

local function safeEquipTool(toolName)
    pcall(function()
        local char = LocalPlayer.Character
        if not char or not char:FindFirstChild("Humanoid") then return end
        local tool = LocalPlayer.Backpack:FindFirstChild(toolName) or char:FindFirstChild(toolName)
        if tool and tool.Parent ~= char then
            char.Humanoid:EquipTool(tool)
        end
    end)
end

local function startAutoRep(toolName, stateKey)
    task.spawn(function()
        while states[stateKey] and getgenv().NexusRunning do
            pcall(function()
                local char = LocalPlayer.Character
                if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then
                    task.wait(1.6)
                    return
                end
                safeEquipTool(toolName)
                if muscleEvent then
                    muscleEvent:FireServer("rep")
                end
            end)
            task.wait(0.09)
        end
        -- Unequip
        pcall(function()
            local char = LocalPlayer.Character
            if char and char:FindFirstChild(toolName) then
                char[toolName].Parent = LocalPlayer.Backpack
            end
        end)
    end)
end

-- Infinite Jump
local function toggleInfiniteJump(state)
    states.InfiniteJump = state
    if state then
        if jumpConn then return end
        jumpConn = UserInputService.JumpRequest:Connect(function()
            pcall(function()
                local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
            end)
        end)
    else
        if jumpConn then jumpConn:Disconnect() jumpConn = nil end
    end
end

-- Anti AFK
local function toggleAntiAfk(state)
    states.AntiAFK = state
    if state then
        if antiAfkConn then return end
        antiAfkConn = LocalPlayer.Idled:Connect(function()
            pcall(function()
                VirtualUser:CaptureController()
                VirtualUser:ClickButton2(Vector2.new())
            end)
        end)
    else
        if antiAfkConn then antiAfkConn:Disconnect() antiAfkConn = nil end
    end
end

-- Tabs
local mainTab = window:AddTab("Main")
local teleportTab = window:AddTab("Teleport")

-- Tools Folder
local toolsFolder = mainTab:AddFolder("Tools")
local rockFarmFolder = mainTab:AddFolder("Rock Farm")

-- Tools toggles
toolsFolder:AddSwitch("Auto Pushups", function(s)
    states.AutoPushups = s
    if s then startAutoRep("Pushups", "AutoPushups") end
end)
toolsFolder:AddSwitch("Auto Weight", function(s)
    states.AutoWeight = s
    if s then startAutoRep("Weight", "AutoWeight") end
end)
toolsFolder:AddSwitch("Auto Handstands", function(s)
    states.AutoHandstands = s
    if s then startAutoRep("Handstands", "AutoHandstands") end
end)
toolsFolder:AddSwitch("Auto Punch (Fast)", function(s)
    states.FastPunch = s
    if s then
        task.spawn(function()
            while states.FastPunch and getgenv().NexusRunning do
                pcall(function()
                    if muscleEvent then
                        muscleEvent:FireServer("punch", "rightHand")
                        muscleEvent:FireServer("punch", "leftHand")
                    end
                    local char = LocalPlayer.Character
                    if char then
                        local punch = char:FindFirstChild("Punch") or LocalPlayer.Backpack:FindFirstChild("Punch")
                        if punch and punch.Parent ~= char then
                            char.Humanoid:EquipTool(punch)
                        end
                        if punch and punch.Parent == char then
                            local atk = punch:FindFirstChild("attackTime")
                            if atk and atk:IsA("NumberValue") then atk.Value = 0.01 end
                            punch:Activate()
                        end
                    end
                end)
                task.wait(0.085)
            end
        end)
    end
end)
toolsFolder:AddButton("Boost FPS (Lag Fix)", function()
    Lighting.GlobalShadows = false
    Lighting.Brightness = 1
    Lighting.ClockTime = 12
    Lighting.FogEnd = 9999
    Lighting.FogStart = 0
    for _, v in pairs(Workspace:GetDescendants()) do
        if v:IsA("ParticleEmitter") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") or
           v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("DepthOfFieldEffect") or v:IsA("SunRaysEffect") then
            v.Enabled = false
        end
    end
end)
toolsFolder:AddButton("Auto Lift Gamepass (Free)", function()
    pcall(function()
        local gpFolder = ReplicatedStorage:FindFirstChild("gamepassIds")
        if not gpFolder then return end
        local owned = LocalPlayer:FindFirstChild("ownedGamepasses") or Instance.new("Folder")
        owned.Name = "ownedGamepasses"
        owned.Parent = LocalPlayer
        for _, gp in pairs(gpFolder:GetChildren()) do
            if gp:IsA("IntValue") then
                local val = Instance.new("IntValue")
                val.Name = gp.Name
                val.Value = gp.Value
                val.Parent = owned
            end
        end
    end)
end)

-- Teleport Buttons
local teleportButtons = {
    {"To Spawn / Safe Area", Vector3.new(-6, 5, -196)},
    {"To Secret Area", Vector3.new(1947, 2, 6191)},
    {"To Tiny Area", Vector3.new(-39, 6, 1886)},
    {"To Frozen Area", Vector3.new(-2623, 6, -409)},
    {"To Mythical Area", Vector3.new(2251, 6, 1073)},
    {"To Inferno Area", Vector3.new(-6759, 6, -1285)},
    {"To Legend Area", Vector3.new(4603, 990, -3898)},
    {"To Muscle King Area", Vector3.new(-8626, 16, -5730)},
    {"To Jungle Area", Vector3.new(-8642, 6, 2381)},
    {"To Lava Brawl", Vector3.new(4471, 119, -8836)},
    {"To Desert Brawl", Vector3.new(960, 17, -7398)},
    {"To Beach Brawl", Vector3.new(-1849, 20, -6335)}
}
for _, btn in ipairs(teleportButtons) do
    local name, pos = btn[1], btn[2]
    teleportTab:AddButton(name, function() tpTo(pos) end)
end

-- Rock Farm Controls
local function setRockFarm(durability, enable)
    if enable then
        getgenv().selectedRockDurability = durability
        getgenv().rockFarmActive = true
    else
        if getgenv().selectedRockDurability == durability then
            getgenv().rockFarmActive = false
            getgenv().selectedRockDurability = 0
        end
    end
end

local rockFarmOptions = {
    {"Farm Tiny Island Rock (0)", 0},
    {"Farm Starter Island Rock (100)", 100},
    {"Farm Legend Beach Rock (5000)", 5000},
    {"Farm Frost Gym Rock (150k)", 150000},
    {"Farm Mythical Gym Rock (400k)", 400000},
    {"Farm Eternal Gym Rock (750k)", 750000},
    {"Farm Legend Gym Rock (1M)", 1000000},
    {"Farm Muscle King Gym Rock (5M)", 5000000},
    {"Farm Ancient Jungle Rock (10M)", 10000000}
}
for _, option in ipairs(rockFarmOptions) do
    local name, dur = option[1], option[2]
    rockFarmFolder:AddSwitch(name, function(s) setRockFarm(dur, s) end)
end
rockFarmFolder:AddButton("Stop All Rock Farming", function()
    getgenv().rockFarmActive = false
    getgenv().selectedRockDurability = 0
end)

-- Rock Farming Loop
task.spawn(function()
    while getgenv().NexusRunning do
        if not getgenv().rockFarmActive or getgenv().selectedRockDurability <= 0 then
            task.wait(0.8)
            continue
        end
        pcall(function()
            local char = LocalPlayer.Character
            if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then
                task.wait(1.4)
                return
            end
            local dur = LocalPlayer:FindFirstChild("Durability")
            if not dur or dur.Value < getgenv().selectedRockDurability then
                task.wait(0.6)
                return
            end
            safeEquipTool("Punch")
            local machinesFolder = Workspace:FindFirstChild("machinesFolder")
            if machinesFolder then
                for _, obj in pairs(machinesFolder:GetDescendants()) do
                    if obj.Name == "neededDurability" and obj:IsA("IntValue") and obj.Value == getgenv().selectedRockDurability then
                        local rock = obj.Parent:FindFirstChild("Rock")
                        if rock and rock:IsA("BasePart") then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                if char:FindFirstChild("RightHand") then
                                    firetouchinterest(rock, hrp, 0)
                                    task.wait(0.03)
                                    firetouchinterest(rock, hrp, 1)
                                end
                                if char:FindFirstChild("LeftHand") then
                                    firetouchinterest(rock, hrp, 0)
                                    task.wait(0.03)
                                    firetouchinterest(rock, hrp, 1)
                                end
                                -- Punch
                                if muscleEvent then
                                    muscleEvent:FireServer("punch", "leftHand")
                                    muscleEvent:FireServer("punch", "rightHand")
                                end
                            end
                        end
                    end
                end
            end
        end)
        task.wait(0.18)
    end
end)

local rebirthTab = window:AddTab("Rebirth")

-- Lock Position / Rebirth Control in Farming tab
local lockEnabled = false
local function stopLock()
    if lockConn then lockConn:Disconnect() lockConn = nil end
    local r = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if r then r.Anchored = false end
end
local function startLock()
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    local currentPos = root.CFrame
    root.Anchored = true
    lockConn = RunService.Heartbeat:Connect(function()
        local r = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if r then
            r.CFrame = currentPos
        end
    end)
end
local function updatePosLock(state)
    lockEnabled = state
    if state then startLock() else stopLock() end
end

-- Auto Rebirth
local function autoRebirth()
    task.spawn(function()
        while true do
            game:GetService("ReplicatedStorage").rEvents.rebirthRemote:InvokeServer("rebirthRequest")
            task.wait(0.1)
        end
    end)
end

local autoRebirthSwitch = rebirthTab:AddSwitch("Auto Rebirth (Infinite)", {
    Default = false,
    Callback = function(val)
        if val then
            autoRebirth()
        end
    end
})

-- Auto Size 1
local autoSizeSwitch = rebirthTab:AddSwitch("Auto Size 1", {
    Default = false,
    Callback = function(val)
        _G.autoSizeActive = val
        if val then
            task.spawn(function()
                while _G.autoSizeActive do
                    game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer("changeSize", 1)
                    task.wait(0.2)
                end
            end)
        end
    end
})

-- Auto Teleport to Muscle King
local function autoTeleportToMuscleKing()
    _G.teleportActive = true
    task.spawn(function()
        while _G.teleportActive do
            if game.Players.LocalPlayer.Character then
                game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-8646, 17, -5738))
            end
            task.wait(0.5)
        end
    end)
end

local function stopAutoTeleport()
    _G.teleportActive = false
end

local autoTeleportSwitch = rebirthTab:AddSwitch("Auto Teleport to Muscle King", {
    Default = false,
    Callback = function(val)
        if val then
            autoTeleportToMuscleKing()
        else
            stopAutoTeleport()
        end
    end
})

rebirthTab:AddLabel("Pack Farming").TextSize = 24

-- Add switches, buttons, dropdowns, etc.
-- Example: Adding a switch with callback
rebirthTab:AddSwitch("Fast Rebirth (Pack Farm)", function(p79)
    fastRebirth = p79
    if p79 then
        -- Your existing code for fast rebirth
        spawn(function()
            local _ReplicatedStorage = game:GetService('ReplicatedStorage')
            local _LocalPlayer = game:GetService('Players').LocalPlayer

            local function unequipAllPets()
                local petsFolder = _LocalPlayer.petsFolder
                for _, pet in pairs(petsFolder:GetChildren()) do
                    if pet:IsA('Folder') then
                        for _, v in pairs(pet:GetChildren()) do
                            _ReplicatedStorage.rEvents.equipPetEvent:FireServer('unequipPet', v)
                        end
                    end
                end
            end

            local function equipPetByName(petName)
                unequipAllPets()
                task.wait(0.01)
                for _, pet in pairs(_LocalPlayer.petsFolder.Unique:GetChildren()) do
                    if pet.Name == petName then
                        _ReplicatedStorage.rEvents.equipPetEvent:FireServer('equipPet', pet)
                    end
                end
            end

            local function getMachineByName(name)
                local v100
                local function searchInFolder(folder)
                    for _, v in pairs(folder:GetChildren()) do
                        if v.Name == name then
                            v100 = v
                            break
                        elseif v:IsA("Folder") then
                            searchInFolder(v)
                            if v100 then break end
                        end
                    end
                end
                searchInFolder(workspace)
                return v100
            end

            local function pressE()
                local VirtualInputManager = game:GetService('VirtualInputManager')
                VirtualInputManager:SendKeyEvent(true, 'E', false, game)
                task.wait(0.1)
                VirtualInputManager:SendKeyEvent(false, 'E', false, game)
            end

            while fastRebirth do
                local rebirthCost = 10000 + 5000 * _LocalPlayer.leaderstats.Rebirths.Value
                if _LocalPlayer.ultimatesFolder:FindFirstChild('Golden Rebirth') then
                    local val = _LocalPlayer.ultimatesFolder['Golden Rebirth'].Value
                    rebirthCost = math.floor(rebirthCost * (1 - val * 0.1))
                end

                -- Rebirth cycle
                unequipAllPets()
                task.wait(0.1)
                equipPetByName('Swift Samurai')

                while _LocalPlayer.leaderstats.Strength.Value < rebirthCost do
                    for _ = 1, 15 do
                        _LocalPlayer.muscleEvent:FireServer('rep')
                    end
                    task.wait()
                end

                -- Equip another pet
                unequipAllPets()
                task.wait(0.1)
                equipPetByName('Tribal Overlord')

                -- Find Jungle Bar Lift
                local jungleLift = getMachineByName('Jungle Bar Lift')
                if jungleLift and jungleLift:FindFirstChild('interactSeat') then
                    _LocalPlayer.Character.HumanoidRootPart.CFrame = jungleLift.interactSeat.CFrame * CFrame.new(0, 3, 0)
                    repeat
                        pressE()
                        task.wait(0.1)
                    until _LocalPlayer.Character.Humanoid.Sit
                end

                -- Rebirth request loop
                repeat
                    game:GetService("ReplicatedStorage").rEvents.rebirthRemote:InvokeServer('rebirthRequest')
                    task.wait(0.1)
                until _LocalPlayer.leaderstats.Rebirths.Value > targetRebirths
            end
        end)
    end
end)

-- Add label "Pack Farming" in rebirth tab
rebirthTab:AddLabel("Pack Farming").TextSize = 24

-- Additional features follow similarly...
-- Ensure that all the UI elements refer to `rebirthTab` and not `rebirth` or other undefined variables.

-- Example: Lock position button
local isLocked = false
local lockConnection = nil

local function getHRP()
    if LocalPlayer and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        return LocalPlayer.Character.HumanoidRootPart
    end
    return nil
end

local function setPositionLock(en)
    local hrp = getHRP()
    if not hrp then return end
    if en then
        local currentCFrame = hrp.CFrame
        hrp.Anchored = true
        if lockConnection then lockConnection:Disconnect() end
        lockConnection = RunService.Heartbeat:Connect(function()
            local r = getHRP()
            if r then
                r.CFrame = currentCFrame
            end
        end)
        isLocked = true
    else
        if hrp then hrp.Anchored = false end
        if lockConnection then lockConnection:Disconnect() end
        lockConnection = nil
        isLocked = false
    end
end

rebirthTab:AddButton("Lock Position", function()
    setPositionLock(not isLocked)
end)

-- Change time of day dropdown
local timeDropdown = rebirthTab:AddDropdown("Change Time", function(selection)
    local lighting = game:GetService("Lighting")
    lighting.Brightness = 2
    lighting.FogEnd = 100000
    lighting.Ambient = Color3.fromRGB(127,127,127)
    if selection == "Night" then
        lighting.ClockTime = 0
    elseif selection == "Day" then
        lighting.ClockTime = 12
    elseif selection == "Midnight" then
        lighting.ClockTime = 6
    elseif selection == "Morning" then
        lighting.ClockTime = 6
    elseif selection == "Noon" then
        lighting.ClockTime = 12
    elseif selection == "Afternoon" then
        lighting.ClockTime = 16
    elseif selection == "Sunset" then
        lighting.ClockTime = 18
        lighting.FogEnd = 500
    elseif selection == "Night" then
        lighting.ClockTime = 20
        lighting.FogEnd = 800
    elseif selection == "Dawn" then
        lighting.ClockTime = 4
        lighting.Brightness = 1.8
        lighting.Ambient = Color3.fromRGB(180, 180, 220)
    elseif selection == "Early Morning" then
        lighting.ClockTime = 2
        lighting.Brightness = 1.2
        lighting.Ambient = Color3.fromRGB(100, 120, 180)
    end
end)
timeDropdown:Add("Night")
timeDropdown:Add("Day")
timeDropdown:Add("Midnight")
timeDropdown:Add("Morning")
timeDropdown:Add("Noon")
timeDropdown:Add("Afternoon")
timeDropdown:Add("Sunset")
timeDropdown:Add("Night")
timeDropdown:Add("Dawn")
timeDropdown:Add("Early Morning")

-- Auto Egg Consumption Logic
local autoEatEgg60 = false
local function eatProteinEgg()
    local player = game.Players.LocalPlayer
    local backpack = player:WaitForChild("Backpack")
    local character = player.Character or player.CharacterAdded:Wait()
    local egg = backpack:FindFirstChild("Protein Egg")
    if egg then
        egg.Parent = character
        pcall(function()
            egg:Activate()
        end)
        print("[AutoEgg] Protein Egg consumed.")
    else
        warn("[AutoEgg] No Protein Egg in Backpack.")
    end
end

task.spawn(function()
    while true do
        if autoEatEgg60 then
            eatProteinEgg()
            task.wait(3600)
        else
            task.wait(1)
        end
    end
end)

rebirthTab:AddSwitch("Eat Egg (60 Min)", function(state)
    autoEatEgg60 = state
    print(state and "[AutoEgg] Activated." or "[AutoEgg] Deactivated.")
end)

local autoEatEgg30 = false
local function eatProteinEggShort()
    local player = game.Players.LocalPlayer
    local backpack = player:WaitForChild("Backpack")
    local character = player.Character or player.CharacterAdded:Wait()
    local egg = backpack:FindFirstChild("Protein Egg")
    if egg then
        egg.Parent = character
        pcall(function()
            egg:Activate()
        end)
        print("[AutoEgg] Protein Egg consumed.")
    else
        warn("[AutoEgg] No Protein Egg in Backpack.")
    end
end

task.spawn(function()
    while true do
        if autoEatEgg30 then
            eatProteinEggShort()
            task.wait(1800)
        else
            task.wait(1)
        end
    end
end)

rebirthTab:AddSwitch("Eat Egg (30 Min)", function(state)
    autoEatEgg30 = state
    print(state and "[AutoEgg] Activated." or "[AutoEgg] Deactivated.")
end)

