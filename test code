--// UI + Features Script //--

local player = game.Players.LocalPlayer
local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
gui.Name = "InvinciblePrivateKillingUI"

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 650, 0, 500)
frame.Position = UDim2.new(0.5, -325, 0.5, -250)
frame.BackgroundColor3 = Color3.fromRGB(0, 10, 0)
frame.BorderSizePixel = 0

local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1, 0, 0, 50)
title.BackgroundColor3 = Color3.fromRGB(0, 20, 0)
title.TextColor3 = Color3.new(1, 1, 1)
title.Text = "Invincible Private Killing"
title.Font = Enum.Font.SourceSansBold
title.TextSize = 20

local yOffset = 60

local function createButton(text, yPos)
    local btn = Instance.new("TextButton", frame)
    btn.Size = UDim2.new(0, 300, 0, 40)
    btn.Position = UDim2.new(0, 20, 0, yPos)
    btn.Text = text
    btn.BackgroundColor3 = Color3.fromRGB(0, 50, 0)
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.SourceSans
    btn.TextSize = 16
    return btn
end

local function createInput(placeholder, yPos)
    local input = Instance.new("TextBox", frame)
    input.Size = UDim2.new(0, 250, 0, 40)
    input.Position = UDim2.new(0, 350, 0, yPos)
    input.PlaceholderText = placeholder
    input.Text = ""
    input.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    input.TextColor3 = Color3.new(1, 1, 1)
    input.Font = Enum.Font.SourceSans
    input.TextSize = 14
    return input
end

-- Initialize feature toggle variables
local AutoFarmEnabled = true
local AutoServerHopEnabled = true
local ServerHopInterval = 30
local KillOnlyWeakerEnabled = true
local AutoTPKingGymEnabled = true
local AutoLockPositionEnabled = true
local AutoSetNightTimeEnabled = true
local AutoBigHitboxEnabled = true

-- Create toggles/buttons/inputs
local function initUI()
    local yPos = 60

    -- Auto Farm Toggle
    local autoFarmBtn = createButton("Auto Farm: ON", yPos)
    local autoFarmState = true
    autoFarmBtn.MouseButton1Click:Connect(function()
        autoFarmState = not autoFarmState
        autoFarmBtn.Text = "Auto Farm: " .. (autoFarmState and "ON" or "OFF")
        AutoFarmEnabled = autoFarmState
    end)

    yPos = yPos + 50

    -- Auto Server Hop Toggle
    local serverHopBtn = createButton("Auto Server Hop: ON", yPos)
    local autoServerHop = true
    serverHopBtn.MouseButton1Click:Connect(function()
        autoServerHop = not autoServerHop
        serverHopBtn.Text = "Auto Server Hop: " .. (autoServerHop and "ON" or "OFF")
        AutoServerHopEnabled = autoServerHop
    end)

    yPos = yPos + 50

    -- Hop Interval Slider (simulate with cycle)
    local hopIntervalLabel = createButton("Hop Interval (s): 30", yPos)
    local options = {5,10,15,20,30,60,120}
    local currentIndex = table.find(options, ServerHopInterval) or 5
    hopIntervalLabel.MouseButton1Click:Connect(function()
        currentIndex = currentIndex + 1
        if currentIndex > #options then currentIndex = 1 end
        ServerHopInterval = options[currentIndex]
        hopIntervalLabel.Text = "Hop Interval (s): " .. ServerHopInterval
    })

    yPos = yPos + 50

    -- Kill Only Weaker Toggle
    local killWeakerBtn = createButton("Kill Only Weaker: ON", yPos)
    local KillOnlyWeaker = true
    killWeakerBtn.MouseButton1Click:Connect(function()
        KillOnlyWeaker = not KillOnlyWeaker
        killWeakerBtn.Text = "Kill Only Weaker: " .. (KillOnlyWeaker and "ON" or "OFF")
        KillOnlyWeakerEnabled = KillOnlyWeaker
    end)

    yPos = yPos + 50

    -- TP Muscle King Gym Button
    local tpGymBtn = createButton("TP Muscle King Gym", yPos)
    tpGymBtn.MouseButton1Click:Connect(function()
        local hrp = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp then hrp.CFrame = gymCFrame end
    end)

    yPos = yPos + 50

    -- Set Night Time Button
    local nightBtn = createButton("Set Night Time", yPos)
    nightBtn.MouseButton1Click:Connect(setNightTime)

    yPos = yPos + 50

    -- Auto Lock Position Toggle
    local lockPosBtn = createButton("Auto Lock Position: ON", yPos)
    local AutoLockPos = true
    lockPosBtn.MouseButton1Click:Connect(function()
        AutoLockPos = not AutoLockPos
        lockPosBtn.Text = "Auto Lock Position: " .. (AutoLockPos and "ON" or "OFF")
        AutoLockPositionEnabled = AutoLockPos
        if AutoLockPos then setupPositionLock() else clearPositionLock() end
    end)

    yPos = yPos + 50

    -- Auto Set Night Time Toggle
    local nightToggleBtn = createButton("Auto Set Night Time: ON", yPos)
    local AutoNight = true
    nightToggleBtn.MouseButton1Click:Connect(function()
        AutoNight = not AutoNight
        nightToggleBtn.Text = "Auto Set Night Time: " .. (AutoNight and "ON" or "OFF")
        AutoSetNightTimeEnabled = AutoNight
    end)

    yPos = yPos + 50

    -- Auto Big Hitbox Toggle
    local bigHitboxBtn = createButton("Auto Big Hitbox: ON", yPos)
    local AutoBigHitbox = true
    bigHitboxBtn.MouseButton1Click:Connect(function()
        AutoBigHitbox = not AutoBigHitbox
        bigHitboxBtn.Text = "Auto Big Hitbox: " .. (AutoBigHitbox and "ON" or "OFF")
        AutoBigHitboxEnabled = AutoBigHitbox
        if AutoBigHitbox then setBigHitbox() else resetHitbox() end
    end)
end

initUI()

--// Existing game logic and functions //--

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer

if getgenv().AutoKillLoaded then return end
getgenv().AutoKillLoaded = true

local LastServerHop = os.clock()

-- Position Lock setup
local positionLockAlignPos, positionLockAttachment

local function clearPositionLock()
    if positionLockAlignPos then positionLockAlignPos:Destroy() positionLockAlignPos = nil end
    if positionLockAttachment then positionLockAttachment:Destroy() positionLockAttachment = nil end
end

local function setupPositionLock()
    clearPositionLock()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    pcall(function() hrp:SetNetworkOwner(LocalPlayer) end)
    positionLockAttachment = Instance.new("Attachment", hrp)
    positionLockAlignPos = Instance.new("AlignPosition")
    positionLockAlignPos.Attachment0 = positionLockAttachment
    positionLockAlignPos.Position = gymPos
    positionLockAlignPos.MaxForce = 1e7
    positionLockAlignPos.MaxVelocity = math.huge
    positionLockAlignPos.Responsiveness = 200
    positionLockAlignPos.RigidityEnabled = true
    positionLockAlignPos.Parent = hrp
end

local function setNightTime()
    Lighting.ClockTime = 0
    Lighting.Brightness = 0.5
    Lighting.Ambient = Color3.fromRGB(50,50,70)
    Lighting.OutdoorAmbient = Color3.fromRGB(30,30,50)
end

local TARGET_HITBOX_SIZE = Vector3.new(25, 25, 25)

local function setBigHitbox()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then hrp.Size = TARGET_HITBOX_SIZE end
end

local function resetHitbox()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then hrp.Size = Vector3.new(2, 2, 1) end
end

local function GetPlayerStatValue(plr, statNames)
    if not plr then return nil end
    if type(statNames) == "string" then statNames = {statNames} end
    for _, name in ipairs(statNames) do
        local val = plr:GetAttribute(name)
        if val ~= nil then return tonumber(val) end
    end
    local stats = plr:FindFirstChild("leaderstats")
    if stats then
        for _, name in ipairs(statNames) do
            local v = stats:FindFirstChild(name)
            if v and v:IsA("ValueBase") then return tonumber(v.Value) end
        end
    end
    return nil
end

local function GetLocalPlayerDamage()
    return GetPlayerStatValue(LocalPlayer, {"Damage", "DMG", "Attack", "Strength", "Str"}) or 1
end

local function GetTargetHealth(target)
    local hum = target.Character and target.Character:FindFirstChildOfClass("Humanoid")
    return hum and hum.MaxHealth or 100
end

local function ShouldKillPlayer(target)
    if not KillOnlyWeakerEnabled then return true end
    local myDmg = GetLocalPlayerDamage()
    local theirHP = GetTargetHealth(target)
    return math.ceil(theirHP / myDmg) <= 5
end

local Character, Humanoid, Hand, Punch, HRP
local LastAttack = 0

local function UpdateAll()
    Character = LocalPlayer.Character
    if not Character then return end
    Humanoid = Character:FindFirstChildOfClass("Humanoid")
    Hand = Character:FindFirstChild("LeftHand") or Character:FindFirstChild("Left Arm")
    Punch = Character:FindFirstChild("Punch")
    HRP = Character:FindFirstChild("HumanoidRootPart")
end

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(2)
    UpdateAll()
    if AutoTPKingGymEnabled and HRP then
        HRP.CFrame = gymCFrame
        task.wait(0.5)
    end
    if AutoLockPositionEnabled then
        task.wait(0.5)
        setupPositionLock()
    end
    if AutoSetNightTimeEnabled then
        task.wait(0.3)
        setNightTime()
    end
    if AutoBigHitboxEnabled then
        task.spawn(function()
            task.wait(0.8)
            setBigHitbox()
        end)
    end
end)

UpdateAll()

-- Anti-AFK
LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

-- Server hop function
local function ServerHop()
    local placeId = game.PlaceId
    local myJobId = game.JobId
    for attempt = 1, 6 do
        local success, response = pcall(function()
            return HttpService:JSONDecode(game:HttpGet(
                "https://games.roblox.com/v1/games/" .. placeId .. "/servers/Public?sortOrder=Desc&limit=100"
            ))
        end)
        if not success or not response or not response.data then
            task.wait(2 + attempt)
            continue
        end
        local candidates = {}
        for _, srv in response.data do
            if srv.id == myJobId then continue end
            if srv.playing >= srv.maxPlayers then continue end
            if srv.playing < 8 then continue end
            local fill = srv.playing / srv.maxPlayers
            if fill > 0.25 and fill < 0.9 then
                table.insert(candidates, srv)
            end
        end
        if #candidates == 0 then
            task.wait(3 + attempt * 1.5)
            continue
        end
        local target = candidates[math.random(1, #candidates)]
        pcall(function()
            TeleportService:TeleportToPlaceInstance(placeId, target.id, LocalPlayer)
        end)
        task.wait(5 + attempt * 2)
    end
    warn("Server hop exhausted attempts")
end

-- Main loop
RunService.Heartbeat:Connect(function()
    -- Server Hop
    if AutoServerHopEnabled and (os.clock() - LastServerHop) >= ServerHopInterval then
        LastServerHop = os.clock()
        task.spawn(ServerHop)
    end

    -- Night refresh
    if AutoSetNightTimeEnabled and math.floor(os.clock()) % 5 == 0 then
        setNightTime()
    end

    -- Hitbox refresh
    if AutoBigHitboxEnabled and math.floor(os.clock() * 10) % 3 == 0 then
        setBigHitbox()
    end

    -- Auto lock position
    if AutoLockPositionEnabled and HRP then
        if positionLockAlignPos then
            positionLockAlignPos.Position = gymPos
        end
        -- fallback force reposition
        if math.floor(os.clock() * 20) % 3 == 0 then
            HRP.CFrame = CFrame.new(gymPos) * HRP.CFrame.Rotation
        end
    end

    UpdateAll()

    if not AutoFarmEnabled or not Character or not Humanoid or Humanoid.Health <= 0 then return end

    if not Punch then
        local tool = LocalPlayer.Backpack:FindFirstChild("Punch")
        if tool then Humanoid:EquipTool(tool) end
        return
    end

    if (os.clock() - LastAttack) < 0.08 then return end
    LastAttack = os.clock()

    -- Attack
    local attackTime = Punch:FindFirstChild("attackTime")
    if attackTime then attackTime.Value = 0 end
    Punch:Activate()

    for _, other in pairs(Players:GetPlayers()) do
        if other == LocalPlayer then continue end
        if not ShouldKillPlayer(other) then continue end
        local char = other.Character
        if not char then continue end
        local hum = char:FindFirstChildOfClass("Humanoid")
        local head = char:FindFirstChild("Head")
        local root = char:FindFirstChild("HumanoidRootPart")
        if hum and head and root and hum.Health > 0 and Hand then
            local wasAnchored = root.Anchored
            root.Anchored = true
            firetouchinterest(head, Hand, 0)
            task.wait()
            firetouchinterest(head, Hand, 1)
            root.Anchored = wasAnchored
        end
    end
end)

print("Invincible Kill Farm Loaded! | Night: " .. (AutoSetNightTimeEnabled and "ON" or "OFF") ..
    " | Big Hitbox: " .. (AutoBigHitboxEnabled and "ON" or "OFF") ..
    " | Hop Interval: " .. ServerHopInterval .. "s")

-- UI controls already set up, now define the functions used in buttons

local gymCFrame = CFrame.new(-8625.93262, 17.2325287, -5730.47217, 0.765763462, -1.84813775e-09, 0.643122315, -1.32089262e-09, 1, 4.44647785e-09, -0.643122315, -4.25444568e-09, 0.765763462)

function setNightTime()
    Lighting.ClockTime = 0
    Lighting.Brightness = 0.5
    Lighting.Ambient = Color3.fromRGB(50,50,70)
    Lighting.OutdoorAmbient = Color3.fromRGB(30,30,50)
end

function setupPositionLock()
    clearPositionLock()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    pcall(function() hrp:SetNetworkOwner(LocalPlayer) end)
    positionLockAttachment = Instance.new("Attachment", hrp)
    positionLockAlignPos = Instance.new("AlignPosition")
    positionLockAlignPos.Attachment0 = positionLockAttachment
    positionLockAlignPos.Position = gymPos
    positionLockAlignPos.MaxForce = 1e7
    positionLockAlignPos.MaxVelocity = math.huge
    positionLockAlignPos.Responsiveness = 200
    positionLockAlignPos.RigidityEnabled = true
    positionLockAlignPos.Parent = hrp
end

function clearPositionLock()
    if positionLockAlignPos then positionLockAlignPos:Destroy() positionLockAlignPos = nil end
    if positionLockAttachment then positionLockAttachment:Destroy() positionLockAttachment = nil end
end

function setBigHitbox()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then hrp.Size = TARGET_HITBOX_SIZE end
end

function resetHitbox()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then hrp.Size = Vector3.new(2, 2, 1) end
end

-- Make the GUI draggable
title.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = frame.Position
    end
end)

title.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X,
            startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

game:GetService("UserInputService").InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)
